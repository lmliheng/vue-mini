export declare const patchProp: (el: Element, key: any, prevValue: any, nextValue: any) => void;
/**
* @判断是否用DOMproperity还是setAttribute
 */
export declare function shouldSetAsProp(el: Element, key: string): boolean;
